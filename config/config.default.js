'use strict';

module.exports = appInfo => {
  const config = {};

  // should change to your own
  config.keys = appInfo.name + '_1498619025559_3871';

  // add your config here

  return config;
};
